import styled from 'styled-components';

const StyledPage = styled.div`
  .page {
  }
`;

const StyledNav = styled.nav`
  display: flex;
  justify-content: start;
  a {
    font-size: 1.3vw;
    padding: 0.5vw 2vw;
    text-decoration: none;
  }
`;

export { StyledPage, StyledNav };
